[project]
name = "booking-api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic-settings>=2.2",
  "prometheus-client>=0.20",
]

[tool.ruff]
line-length = 100
